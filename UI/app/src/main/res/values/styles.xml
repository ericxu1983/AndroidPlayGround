<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="SeparateLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.51dp</item>
        <item name="android:background">#e8e9eb</item>
    </style>

    <style name="RadioButtonAsSwitchBar">
        <item name="android:width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/background_radio_button</item>
        <item name="android:textColor">@drawable/text_color_radio_button</item>
    </style>

</resources>
